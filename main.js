/*global
  createCanvas,
  background,
*/
// Add functions to this list that give you a red dot.

var width = 400;
var height = 400;

function setup() {
  createCanvas(width, height);
}

function draw() {
  background(200);
}
